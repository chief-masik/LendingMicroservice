spring:
  liquibase:
    enabled: true
  datasource:
    url: jdbc:h2:file:./loan_db
    driverClassName: org.h2.Driver
    username: masik
    password: 111
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none

  h2:
    console:
      enabled: true